lanche = ('Hámburguer' , 'Suco', 'Pizza')
for pos, comida in enumerate(lanche):
    print(f'Comida: {comida} na posição {pos}')
for c in range(0, len(lanche)):
    print(lanche[c])
# Tuplas são imutáveis
